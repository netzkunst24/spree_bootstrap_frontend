<%= link_to 'zurück zum Produkt', @product %>
<%= content_tag :h2, Spree.t(:leave_us_a_review_for, :name => @product.name), :class => 'new-review-title' %>
<div class="row">
    <%= render 'form', :review => @review, :product => @product %>
  <div class="col-sm-6">
  </div>
</div>
